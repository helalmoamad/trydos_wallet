import 'package:flutter/material.dart';

/// تبويب الإعدادات (مكان مؤقت - يُستكمل لاحقاً).
class SettingsTab extends StatelessWidget {
  const SettingsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Settings'),
    );
  }
}
